theme = TokyoNight Night

window-padding-color = extend-always
window-padding-x = 15
window-padding-y = 0

cursor-style = block

shell-integration = zsh
custom-shader = ~/.config/ghostty/shaders/cursor_blaze.glsl

# every new terminal starts in tmux — each window gets its own session
command = tmux
